services:
  - type: web
    name: sih-disaster-management-suite
    env: node
    buildCommand: npm run setup && cd client && npm run build
    startCommand: node server/server.js
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
